export interface Theme {
  id: string;
  name: string;
  background: string;
  textColor: string;
  accentColor: string;
  menuBackground: string;
  menuHoverBackground: string;
  editorBackground: string;
}